rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /user_profile_images/{image} {
      allow read, write: if request.auth != null && request.auth.uid + ".png" == image;
    }
  }
}
